name: Update Dependencies

on:
  schedule:
    - cron: "0 0 * * *" # 毎日 UTC 0:00（JST 9:00）
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: DeterminateSystems/nix-installer-action@main

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      # --- flake inputs ---
      - name: Update flake inputs
        run: nix flake update

      # --- claude-code ---
      - name: Update claude-code
        run: bash scripts/update-claude-code.sh

      # --- commit & push ---
      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add flake.lock overlays/claude-code.nix overlays/claude-code-package-lock.json
          git diff --cached --quiet && echo "Already up to date" && exit 0
          git commit -m "依存パッケージを更新"
          git push
